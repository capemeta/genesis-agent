# PPT 只读入口

只提取并总结绑定输入中的文字、演讲者备注、表格和可读取的图表文字。

- 使用 `run_skill_command` 在 `office-ppt` 包工作区执行 `python -m markitdown <输入别名>`。
- 不创建、修改、渲染或发布 PPT，不调用写文件和候选选择工具。
- 明确披露无法从 OOXML/文本提取器读取的视觉内容；不得把文本提取结果描述成视觉检查。
- 最终返回消息结果，并按用户任务组织摘要。
