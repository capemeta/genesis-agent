# Excel 验证清单

- workbook 能打开，sheet 名称和顺序符合预期。
- 表头、单位、日期、数字格式和币种明确。
- 公式使用单元格引用，避免硬编码可变计算结果。
- 没有 `#REF!`、`#DIV/0!`、`#VALUE!`、`#N/A`、`#NAME?` 等错误。
- 财务模型中输入、公式、跨 sheet 链接和外部链接有清晰颜色或注释约定。
- 修改模板时保留既有样式、命名、冻结窗格、筛选和保护设置。
- CSV/TSV 要检查编码、分隔符、行列错位和多余空行。
- 图片型表格或票据照片需要读字时，使用 `office-ocr`。
