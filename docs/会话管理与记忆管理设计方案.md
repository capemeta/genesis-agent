# 会话管理与记忆管理设计方案

> 状态：设计稿（第一性原理 + 现状盘点 + 参考借鉴 + 目标架构 + 数据模型 + 分期落地）
> 范围：会话（Session）生命周期与持久化、消息历史管理、上下文窗口预算分配、上下文压缩/摘要机制、四层记忆（工作/短期/长期/画像）、长期记忆的形成与存储检索、CLI/Desktop/Enterprise 三产品统一与差异、与多智能体（Agent App）的兼容
> 强相关文档：`docs/agent loop设计.md`（§4.8 Context Window、§11 记忆系统）、`docs/提示词分层设计方案.md`（System Prompt 分层）、`docs/Agent App 智能体应用与多智能体编排设计.md`（多智能体分层）、`docs/子智能体设计.md`、`docs/Hook机制设计方案.md`、`docs/项目目录与边界说明.md`、`docs/产品分发架构设计.md`
> 参考源码：`D:\workspace\go\go-project\Kode-CLI`、`D:\workspace\go\go-project\codex`（完整文件清单见附录 A / B）

---

## 一、设计目标与要回答的问题

会话管理与记忆管理是 Agent Runtime 的第二根内核支柱（第一根是提示词分层，见 `docs/提示词分层设计方案.md`）。提示词决定"把通用内核塑造成哪类工作"，而会话/记忆决定"这类工作在长程、多轮、跨会话中如何保持连贯、如何积累经验、如何在有限上下文里做最优取舍"。

本方案要回答用户提出的核心问题：

1. **记忆分层**：工作记忆、短期记忆、长期记忆、用户画像如何分层？各自生命周期、存储、检索有何不同？（§五）
2. **长期记忆如何形成**：从哪来、抽取什么、如何去重合并、如何支持"不同项目沉淀不同内容"的自定义抽取与结构？（§七）
3. **会话管理**：摘要、压缩、上下文如何组织？完整消息链如何持久化与恢复？（§六）
4. **上下文分配**：历史记录、摘要、长期记忆、全局提示词、工具、当前问题如何在有限窗口内分配？（§八）
5. **提示词构建是否要考虑模型总上下文**：是。需要（§八）：
   - 模型配置文件增加"最大上下文"字段（私有化部署模型难以自动探测，必须可配置）；
   - 一套预算计算公式（预留输出长度后，剩余额度在各部分间按比例分配，但比例可弹性伸缩、带区间限制）；
   - 历史记录取多少条、单条最大长度、最大长度占比的约束。
6. **科学的压缩机制**：超过多少触发压缩？压缩多少轮、保留多少轮？长期记忆在压缩链路的哪个环节沉淀？（§六 + §七）
7. **三产品差异**：CLI / Desktop / Enterprise 在存储与检索上如何统一接口、差异化后端？（§十）
8. **多智能体兼容**：不同 Agent / Agent App / 子智能体 / 协作空间下，会话与记忆如何兼容（至少预留字段、隔离范围）？（§九）

一句话结论（先给出，后文论证）：

> **会话 = 一条可追加、可摘要、可恢复的消息流 + 一份上下文预算契约**；**记忆 = 四层生命周期不同的知识资产**。运行时内核只负责"预算分配 + 上下文装配 + 压缩编排 + 记忆注入顺序"这套与产品无关的算法；消息/记忆/画像的**存储后端**（文件 / SQLite / DB / 向量库）和**检索方式**（关键词 / 结构化 / 语义）通过统一契约接口由产品端口注入。模型最大上下文是可配置字段，预算按"刚性段优先满足 + 弹性段按比例分配并带区间 clamp 与额度回流"的算法平衡。长期记忆通过"可配置抽取器（两阶段：抽取 → 合并）"从会话中沉淀，不同项目可自定义抽取提示词与结构。三端同源算法，异构后端。

---

## 二、第一性原理：会话与记忆到底要解决什么

抛开当前实现，把问题还原到本质。

### 2.1 三个不可回避的物理约束

| 约束 | 本质 | 带来的必然设计 |
| --- | --- | --- |
| **上下文窗口有限** | LLM 单次能看到的 token 有上限（私有化模型甚至更小、且难探测） | 必须有"预算 + 裁剪 + 压缩"，不能把全量历史无脑塞进去 |
| **对话是增量、单调增长的** | 每轮都在累积消息，长会话必然溢出 | 必须有摘要/滑窗/选择性召回，把"旧而重要"的信息用更少 token 表达 |
| **经验值得跨会话复用** | 用户偏好、项目约定、失败教训不该每次从零开始 | 必须有跨会话的长期记忆与画像，且"记什么"是可控、可编辑、可审计的业务决策 |

### 2.2 信息按"寿命"分层（与提示词分层同一方法论）

会话/记忆承载的信息，其**寿命**和**归属**完全不同，必须分层，不能塞进同一个存储或同一段上下文：

| 信息类别 | 寿命 | 存储 | 检索方式 | 谁决定"记什么" |
| --- | --- | --- | --- | --- |
| **工作记忆** | Run 内，结束即释放 | 内存（RunContext） | 直接访问 | 框架 |
| **短期记忆（对话历史）** | Session 生命周期 | 文件/DB/Redis | 按时间顺序 + 摘要 | 框架 |
| **长期记忆（经验）** | 跨 Session，永久（可过期） | 文件/DB/向量库 | 关键词/结构化/语义 | **业务**（框架只负责读取注入与提供抽取器骨架） |
| **用户画像** | 跨 Session，相对稳定 | 文件/DB/画像服务 | 结构化字段读取 | 业务（可编辑可审计，不靠不可解释的 Prompt 总结） |

### 2.3 必须满足的不变量（验收基线）

1. **完整性**：短期记忆必须能保存**完整消息链**（user / assistant / tool_calls / tool result / reasoning），而不是只存 user + 最终回答（当前实现的缺陷）——否则恢复会话、复现工具链、做摘要都缺料。
2. **可恢复**：会话可从持久化介质完整或按摘要边界恢复（resume / fork）。
3. **预算安全**：装进 LLM 的上下文**永不超**模型有效窗口；刚性段（system + 当前问题 + 工具 schema）优先满足；溢出时优雅降级（压缩/裁剪/明确报错），不静默截断关键信息。
4. **压缩可解释、可持久化**：压缩后的历史是"替换"而非"丢失"——摘要作为一条消息留在流里，原始历史在持久层可追溯（借鉴 codex `CompactedItem.replacement_history`）。
5. **记忆可控可编辑**：长期记忆与画像"记什么"由业务决策，且可查看、编辑、删除、审计；`secret/pii` 默认不进上下文。
6. **接口与实现分离**：内核只依赖 `ShortTermMemory` / `LongTermMemory` / `UserProfileStore` / `TokenEstimator` / `SessionStore` 契约；文件/DB/向量库/tiktoken 都是可替换 adapter。
7. **三端同源**：预算算法、装配顺序、压缩编排零 `if product == ...`；差异只经存储/检索/token 后端注入。
8. **多智能体兼容**：Session/Message/Memory 数据模型预留 `app_id`/`agent_instance_id`/`scope`，为 Agent App 与协作空间留出隔离维度，当前阶段只记录不编排。

### 2.4 不做会怎样（失败条件）

- 无预算 → 长会话直接触发模型报错 `context_length_exceeded`，或被网关静默截断丢失关键上下文。
- 无压缩 → 要么频繁报错，要么被迫限制对话轮数，长程任务无法进行。
- 只存 user+final → 无法恢复工具执行链，无法复盘，摘要缺失中间证据。
- 长期记忆由框架自动写 → 记忆库膨胀、污染、不可审计、隐私泄露。
- 存储写死本地文件 → Enterprise 无法多租户隔离、无法向量检索、无法接画像服务。

---

## 三、现状盘点（本项目当前实现与差距）

> 基于代码只读盘点，**代码事实优先于设计文档**。

### 3.1 当前会话/记忆链路

```text
RunOnce(sessionID, input)                         // internal/app/run_service.go
  └─ ReactLoopEngine.Start / loop                 // internal/runtime/strategy/react/react_loop.go:164
       ├─ prompt.BuildSystem()  → system 消息（仅 Run 开始一次）
       ├─ memory.GetHistory(sessionID) → 全量历史，无裁剪、无预算   // :171
       ├─ append 当前 user 输入
       ├─ loop: llm.StreamGenerate(rc.Messages, tools)             // :284
       └─ 有最终回答时：memory.AppendMessages(user, finalAssistant) // :413（仅存两条！）
```

### 3.2 已有资产与缺口总表

| 能力 | 目标 | 现状 | 差距 |
| --- | --- | --- | --- |
| 短期记忆接口 | append / get_recent(limit, budget) / summarize / get_summary / clear | `ShortTermStore`：仅 Append / Get / Clear（`internal/capabilities/memory/contract/interface.go:15`） | 缺预算读取、摘要、压缩 |
| 消息完整性 | 存完整消息链 | 仅存 `user + 最终 assistant`（`react_loop.go:413`） | **丢失 tool_calls / tool result / reasoning** |
| 短期记忆存储 | 文件/DB/Redis 可替换 | 仅 `InMemoryStore`（`adapter/inmemory/inmemory.go`），进程重启即丢 | 缺持久化 + 恢复 |
| 长期记忆 | `LongTermMemory` 接口 + 多后端 | 仅 `domain.MemoryEntry` 结构体（`internal/domain/memory.go`），**无接口无实现** | 全缺 |
| 用户画像 | `UserProfileStore` 接口 | **无接口无实现** | 全缺 |
| Session 模型 | 含 summary 指针/token 统计/状态/app_id/agent_instance_id | `domain.Session` 仅 6 字段（`internal/domain/session.go`） | 缺会话状态与上下文元数据 |
| Session 持久化 | 可查询、可恢复、可列表 | `session_service.go` 只 New/Clear，无存储 | 全缺 |
| 上下文窗口预算 | context_window 字段 + 预算公式 | `config` 只有 `max_tokens`(输出)（`config.go:157`），**无 context_window** | 缺最大上下文字段 + 分配器 |
| max_tokens 接线 | 输出上限传给 LLM | 解析到 `ResolvedLLMConfig.MaxTokens` 但 **eino factory 未使用** | 未接线 |
| Token 计数 | TokenEstimator 端口 | 无；`RunContext.AddTokens` 定义了但**从未调用** | 全缺 |
| ContextBuilder | 预算 + 装配 + 裁剪 | 无；`react_loop` 直接全量拼 | 全缺 |
| 压缩/摘要 | micro-compact + auto-compact | 无对话压缩；`config.yaml` 有 `router.summarization: fast` 但无代码调用 | 全缺 |

### 3.3 方向正确、可复用的部分

- `domain.Message`（`internal/domain/message.go`）已是不依赖外部框架的自有消息模型，含 `ToolCalls / ToolCallID / ReasoningContent`，**足以承载完整消息链**——问题只在持久化时被裁剪。
- `RunContext`（`internal/runtime/context.go`）已有 `Messages []*Message`、`TokenUsed int64`、`AddTokens`，是上下文运行时载体的雏形。
- `domain.MemoryEntry`、`docs/agent loop设计.md §11` 已定义长期记忆的字段方向（scope/importance/decay），本方案沿用并补齐接口。
- `config.LLMModelConfig.MaxTokens` 已在，只需**新增** `ContextWindow` 等字段，不推翻。

---

## 四、参考源码借鉴（Kode-CLI 与 codex）

> 完整文件清单见附录 A（Kode-CLI）、附录 B（codex）。此处只提炼**经第一性原理筛选后确定采纳**的点，并标注对应源码位置，便于开发时直接查阅。

### 4.1 从 codex 采纳

| 借鉴点 | 说明 | 采纳形态 | 关键源码 |
| --- | --- | --- | --- |
| **双层存储** | 内存 `Vec<ResponseItem>` 承载活跃 transcript；追加式 JSONL rollout 持久化；SQLite 存元数据索引 | 短期记忆 = 内存 + JSONL（可重放）+ 索引 | `codex-rs/core/src/context_manager/history.rs:36`、`rollout/src/recorder.rs`、`state/src/lib.rs` |
| **base 与 history 分离** | 系统指令走独立 `base_instructions` 字段，不混进消息 list | 压缩只动 history body，system 稳定可缓存（与提示词分层方案一致） | `codex-rs/core/src/client_common.rs:17` |
| **压缩=替换历史+持久化记录** | `CompactedItem.replacement_history` 保证 resume 一致 | 摘要落 `summary` 记录 + leaf 指针，原始历史在持久层可追溯 | `codex-rs/core/src/compact.rs:585`、`protocol/src/compacted_item.rs` |
| **阈值双轨** | 90% 触发压缩预算 + 95% 有效窗口硬上限；可选 `BodyAfterPrefix` 只计 body 增长 | `compact_threshold`(默认 0.85~0.90) + `effective_context_ratio`(默认 0.9~0.95) | `codex-rs/protocol/src/openai_models.rs:441`、`core/src/session/context_window.rs` |
| **有效窗口比例** | `effective_context_window_percent` 默认 95%，为协议/tokenizer 误差留头 | 模型配置 `effective_context_ratio` 字段 | `codex-rs/core/src/session/turn_context.rs:188` |
| **保留最近 user 消息预算** | 压缩后按 20000 token 预算从新到旧保留 user 消息 | 压缩保留策略用 token 预算 + 轮数上限双约束 | `codex-rs/core/src/compact.rs`（`COMPACT_USER_MESSAGE_MAX_TOKENS`） |
| **结构化摘要提示词** | 独立模板文件 + summary 前缀标记 | 摘要器用可配置模板（结构化分段） | `codex-rs/prompts/templates/compact/prompt.md`、`summary_prefix.md` |
| **记忆两阶段** | Phase1 抽取（per-rollout raw memory）→ Phase2 合并（consolidation 去重） | `MemoryExtractor` 两阶段：抽取 → 合并 | `codex-rs/memories/write/src/phase1.rs`、`phase2.rs`、`templates/memories/*.md` |
| **记忆读时注入 summary** | `memory_summary.md` 作为 developer 片段注入，可选 `memory_search/read` 工具 | 长期记忆检索结果注入上下文；可选记忆检索工具 | `codex-rs/ext/memories/src/extension.rs`、`templates/memories/read_path.md` |
| **token 估算务实** | 无 tiktoken，本地 `bytes/4` 启发式；服务端 usage 为权威计费 | `TokenEstimator` 端口：默认启发式，可选 tiktoken/服务端 usage | `codex-rs/utils/string/src/truncate.rs:71` |
| **上下文 diff** | `reference_context_item` + WorldState patch，避免每 turn 全量重注入 | Phase 3 差量注入目标 | `codex-rs/core/src/context_manager/updates.rs`、`context/world_state/mod.rs` |

### 4.2 从 Kode-CLI 采纳

| 借鉴点 | 说明 | 采纳形态 | 关键源码 |
| --- | --- | --- | --- |
| **双层压缩** | L1 micro-compact：旧工具结果外置到磁盘、原位替换预览；L2 auto-compact：LLM 全量摘要 | 分离"工具输出裁剪"与"对话摘要"两级压缩 | `packages/core/src/utils/microCompactCore.ts`、`autoCompactCore.ts` |
| **可预测预算模型** | `有效窗口 = 模型窗口 - reserve(10%，上限 20k)`；压缩触发 = 有效窗口 - 固定 margin(13k) | 预算= 有效窗口 - 输出预留；阈值用比例+固定 margin 双控 | `packages/core/src/utils/autoCompactThreshold.ts:8` |
| **摘要回填为消息** | 摘要做成 assistant 消息留在 transcript；resume 时按 `leafUuid` 截断 JSONL | 摘要作为一条特殊消息 + summary 记录 + leaf 指针 | `packages/core/src/utils/autoCompactCore.ts:320`、`protocol/src/utils/kodeAgentSessionLoad.ts:370` |
| **8 段结构化摘要** | Technical Context / Project / Code Changes / Debugging / Status / Pending / Preferences / Decisions | 摘要模板结构化分段（保证关键信息不丢） | `packages/core/src/utils/autoCompactCore.ts`（`COMPRESSION_PROMPT_BASE`） |
| **压缩用独立模型** | `main/compact/task/quick` 模型指针分离，压缩用便宜模型 | 复用 `config.yaml router.summarization`（已存在！）选压缩模型 | `packages/core/src/model/manager.ts` |
| **保留最近 tool 结果** | micro-compact 保留最近 3 次工具调用 | micro-compact 保留最近 N 次工具结果不外置 | `microCompactCore.ts`（`KEEP_LAST_TOOL_USES`） |
| **CJK/开销系数** | char/4 + 图片固定 2000 + 4/3 开销系数 | 启发式估算含 CJK 权重与开销系数 | `packages/core/src/utils/tokens.ts` |
| **上下文百分比 UI** | `computeContextWindowPercentages` 供 UI 显示"上下文余量" | 三端 UI 展示上下文使用率（progress 事件） | `apps/cli/src/ui/components/PromptInput/PromptInput.tsx`、`TokenWarning.tsx` |
| **压缩后文件恢复** | 压缩后恢复最近 5 个读过的文件（50k 预算），避免摘要丢代码上下文 | 可选"关键工件恢复"作为压缩后处理 | `packages/core/src/utils/fileRecoveryCore.ts`、`services/fileFreshness.ts` |
| **项目记忆链式发现** | 目录链 `AGENTS.md` + override + 32KB 上限 | 长期记忆的"项目文档"来源（与提示词分层方案 `ProjectDocInjector` 共用） | `packages/core/src/utils/projectInstructions.ts` |
| **# 命令写记忆** | 用户 `#` 前缀把内容追加进 `AGENTS.md` | 用户显式"记住"入口 → 写长期记忆/项目文档 | `packages/core/src/utils/hashCommand.ts` |

### 4.3 刻意不采纳（避免过度设计）

- codex 的 `reference_context_item` 全量差量框架、Remote compaction API——第一轮用全量重注入 + 本地摘要即可，差量留 Phase 3。
- codex `state_5.sqlite` 的完整 threads/jobs 表体系——CLI/Desktop 第一轮用 JSONL + 轻量索引，DB 化留 Enterprise。
- Kode 的 React 全局 getter/setter 消息态——我们用显式 `SessionStore` + `RunContext`，不做全局可变态。
- 两者的"图片固定 2000 token"等魔法常量——参数化为可配置估算系数，不硬编码。

---

## 五、记忆四层模型（分层与职责）

沿用 `docs/agent loop设计.md §11` 的四层分类，本方案补齐**接口契约与形成机制**。

```text
┌─────────────────────────────────────────────────────────────────────────┐
│ L0 工作记忆 Working Memory   —— Run 内，内存，框架直管                       │
│    RunContext.WorkingMemory: current_plan / retrieved_chunks / tool_results │
│    生命周期：Run 结束即释放；无独立接口                                       │
├─────────────────────────────────────────────────────────────────────────┤
│ L1 短期记忆 ShortTermMemory  —— Session 内，可持久化，框架直管               │
│    完整对话消息链 + 滚动摘要(summary) + leaf 指针                            │
│    接口：ShortTermMemory（append/get_recent/summarize/get_summary/clear）    │
│    存储：内存 → 文件 JSONL → DB/Redis                                        │
├─────────────────────────────────────────────────────────────────────────┤
│ L2 长期记忆 LongTermMemory   —— 跨 Session，业务决定记什么，框架负责读注入    │
│    episodic / semantic / procedural / negative；scope=user/agent/workspace  │
│    接口：LongTermMemory（save/search/list/update/delete/consolidate/decay）  │
│    存储：文件(关键词) → DB → 向量库(语义)                                     │
├─────────────────────────────────────────────────────────────────────────┤
│ L3 用户画像 UserProfileStore —— 跨 Session，稳定偏好，可编辑可审计            │
│    locale/communication_style/tool_preferences/risk_preference...           │
│    接口：UserProfileStore（get/update/list_fields/delete_field）             │
│    存储：文件 JSON → DB → CRM/画像服务                                        │
└─────────────────────────────────────────────────────────────────────────┘
```

### 5.1 层间协作（一次 Run 的记忆生命周期）

```text
Run 启动（一次性，缓存到 RunContext）
  1. UserProfileStore.Get(scope)        → 画像片段（注入 system，见提示词分层 L3 user_prefs）
  2. LongTermMemory.Search(user_input)  → 相关经验（注入 system 的 long_term_memories 块）
  3. ShortTermMemory.GetRecent(budget)  → 对话历史（按预算裁剪）+ GetSummary() 摘要

Run 进行中
  - 工作记忆在 RunContext.WorkingMemory 累积（工具结果、plan 进度）
  - 上下文接近阈值 → 触发压缩（§六）

Run 结束（on_run_complete）
  - ShortTermMemory.Append(完整消息链)  // 修正现状：存全链，非仅 user+final
  - （可选，业务）MemoryExtractor 触发长期记忆抽取（§七）
```

> **注入顺序与预算**由 §八的 ContextBudgetPlanner + ContextAssembler 统一编排；本节只定义"有哪些层、各层接口"。

### 5.2 接口契约（Go，落 `internal/capabilities/memory/contract`）

```go
// ShortTermMemory 短期记忆：管理 Session 级完整对话历史与滚动摘要。
// 演进自现有 ShortTermStore（保留 Append/Get/Clear，补齐预算读取与摘要）。
type ShortTermMemory interface {
    Append(ctx context.Context, ref SessionRef, msgs []*domain.Message) error
    // GetRecent 从新到旧返回消息，受条数与 token 预算双约束；返回是否发生截断。
    GetRecent(ctx context.Context, ref SessionRef, opt RecentOptions) (RecentResult, error)
    // Summarize 将 keepRecent 之外的旧历史压缩为摘要并落盘（返回新的 summary 与 leaf 指针）。
    Summarize(ctx context.Context, ref SessionRef, opt SummarizeOptions) (SummaryResult, error)
    GetSummary(ctx context.Context, ref SessionRef) (*domain.SessionSummary, error)
    Clear(ctx context.Context, ref SessionRef) error
}

// SessionRef 所有请求必携带隔离维度（租户/用户/会话 + 多智能体维度）。
type SessionRef struct {
    TenantID        string
    UserID          string
    SessionID       string
    AppID           string // 多智能体：Agent App（可空）
    AgentInstanceID string // 多智能体：Agent 实例（可空）
}

type RecentOptions struct {
    MaxTurns    int   // 条数/轮数上限（0=不限）
    TokenBudget int   // token 预算（0=不限）
    IncludeToolChain bool // 是否包含 tool_calls/tool result 完整链
}

type RecentResult struct {
    Messages  []*domain.Message
    Truncated bool // 是否因预算被裁剪（供 UI/摘要判断）
    UsedTokens int
}

// LongTermMemory 长期记忆：字段对齐 domain.LongTermEntry（§七.4）。
type LongTermMemory interface {
    Save(ctx context.Context, entry *domain.LongTermEntry) (string, error)
    Search(ctx context.Context, q MemoryQuery) ([]*domain.LongTermEntry, error)
    List(ctx context.Context, q MemoryListQuery) (MemoryPage, error)
    Get(ctx context.Context, id string) (*domain.LongTermEntry, error)
    Update(ctx context.Context, id string, patch MemoryPatch) error
    Delete(ctx context.Context, id string) error
    Consolidate(ctx context.Context, scope MemoryScope) error // 合并去重
    Decay(ctx context.Context, scope MemoryScope, now time.Time) error
}

// UserProfileStore 用户画像：可编辑、可审计、字段级敏感度。
type UserProfileStore interface {
    Get(ctx context.Context, scope ProfileScope) (*domain.UserProfile, error)
    Update(ctx context.Context, scope ProfileScope, patch ProfilePatch) (*domain.UserProfile, error)
    ListFields(ctx context.Context, scope ProfileScope) ([]domain.ProfileField, error)
    DeleteField(ctx context.Context, scope ProfileScope, field string) error
}

// SessionStore 会话"元数据"存储，与 ShortTermMemory（消息流）职责分离：
// - ShortTermMemory 管的是"消息本体 + 摘要"；
// - SessionStore 管的是 Session 记录本身（标题、状态、summary_leaf 指针、token 统计、列表检索）。
// 二者可由同一后端实现（如都落文件/同一 DB），但契约分开，便于会话列表/管理端独立演化。
type SessionStore interface {
    Create(ctx context.Context, s *domain.Session) error
    Get(ctx context.Context, ref SessionRef) (*domain.Session, error)
    Update(ctx context.Context, ref SessionRef, patch SessionPatch) error // 更新 status/leaf/token/title
    List(ctx context.Context, q SessionListQuery) (SessionPage, error)     // 用户/管理端分页
    Delete(ctx context.Context, ref SessionRef) error
}
```

- **兼容策略**：现有 `ShortTermStore`（3 方法）作为 `ShortTermMemory` 的最小子集，Phase 1 先把 `react_loop` 对接 `ShortTermMemory`，`InMemoryStore` 升级实现新接口；旧接口在过渡期后删除（对齐"开发阶段不兼容旧代码"）。现有 `internal/app/session_service.go` 的 `NewSession`/`ClearSession` 改为委托 `SessionStore`。
- **依赖方向说明**：以上契约（`ShortTermMemory`/`LongTermMemory`/`UserProfileStore`/`SessionStore`/`MemoryExtractor`）与 `SessionRef` 等类型定义在 `internal/capabilities/memory/contract`，属**端口（接口）包**。运行时（`runtime/strategy/react` 已 import `memory.ShortTermStore`、`runtime/context` 的 Compactor/Assembler）依赖这些**契约端口**是合法的；被禁止的是依赖 `adapter/*` 具体实现与 `products/*`（见 §十一）。

---

## 六、会话管理：消息流、摘要、压缩、恢复

### 6.1 会话生命周期与状态

```text
create → active ⇄ compacting → idle → archived → deleted
```

- `create`：首次 RunOnce 隐式创建，或显式 `NewSession`。
- `active`：有进行中的 Run。
- `compacting`：正在做 auto-compact（防止并发 Run 读到半压缩态，加会话级锁）。
- `idle`：无活跃 Run，超过 TTL（短期记忆默认 7 天）可迁冷。
- `archived / deleted`：Enterprise 合规留存或用户删除。

### 6.2 消息持久化（修正现状最关键的一处）

**现状缺陷**：`react_loop.go:413` 只 `Append(user, finalAssistant)`，丢失 tool_calls / tool result / reasoning。

**目标**：一个 Run 结束时，把 `RunContext.Messages` 中本 Run 新增的**完整消息链**（含 assistant 的 tool_calls、role=tool 的结果、reasoning）追加进短期记忆。

采纳 codex/Kode 的**追加式 JSONL**：每条消息一行，带 envelope（`session_id / run_id / uuid / parent_uuid / role / app_id / agent_instance_id / created_at`）。

```text
{sessionRoot}/{sessionID}.jsonl        # 主消息流（追加写）
{sessionRoot}/{sessionID}.summary.json # 当前滚动摘要 + leaf 指针
{sessionRoot}/{sessionID}/subagents/agent-{instanceID}.jsonl # 子智能体旁链（借鉴 Kode）
```

> JSONL 追加对文件后端天然友好、易调试、易迁移；DB 后端则映射为 `agent_message` 表（`docs/agent loop设计.md §11.3`）。二者都实现同一 `ShortTermMemory` 契约。

### 6.3 恢复（resume / fork）

- **resume**：加载 `.summary.json` 得 leaf 指针，从 JSONL 中 leaf 之后的消息重建历史（借鉴 Kode `loadForResume` + codex `apply_rollout_reconstruction`）；leaf 之前用摘要代表。
- **fork**：复制到新 sessionID，指向同一 leaf，之后独立演化。
- 未压缩过的会话：leaf 为空，全量加载。

### 6.4 两级压缩机制（科学的压缩：触发、保留、回填）

借鉴 Kode 双层 + codex 替换语义，分两级：

#### L1 — Micro-Compact（工具结果卸载，轻量、无 LLM）

- **目的**：大工具输出（文件读取、命令 stdout、检索结果）是 token 消耗大户，先就地瘦身，往往就能避免触发昂贵的 LLM 摘要。
- **触发**：单条工具结果 token > `tool_result_max_tokens`（默认 8k），或上下文使用率 ≥ `warn_ratio`（默认 0.75）。
- **策略**：把旧工具结果正文外置到 `{sessionRoot}/artifacts/`，原位替换为预览 + `<persisted-output ref="...">` 占位；**保留最近 `keep_last_tool_uses` 次（默认 3）**工具结果完整。
- **可逆**：占位保留 ref，模型或用户可按需重新读取（借鉴 Kode `toolResultPersistence`）。

#### L2 — Auto-Compact（对话摘要，LLM 生成）

- **触发**：估算的输入上下文使用率 ≥ `compact_ratio`（默认 0.85，即有效窗口的 85%）且历史轮数 ≥ `min_turns_to_compact`（默认 4，防抖）。
- **压缩多少轮 / 保留多少轮**（轮数是下界、token 是上限）：
  - 保留集 = 从最新往回，**至少**保留 `keep_recent_turns`（默认 6~10 轮，可配）以保证近期连贯；在满足该下界后继续往回多留，直到累计 token 触达 `keep_recent_token_budget`（默认有效窗口的 30%）这一**硬上限**为止。
  - 若仅 `keep_recent_turns` 轮就已超过 token 上限（近期消息很大），先对这些消息走 L1 micro-compact 瘦身，仍超则以轮数下界为准（保连贯优先）。
  - 其余"更老"的历史 **全部**送入摘要器，压成一段结构化摘要。
- **滚动摘要**：若已存在旧摘要，新摘要**吸收**旧摘要（rolling summary），避免摘要丢失更早信息（借鉴 codex summary_prefix 承接语义）。
- **摘要模板**：结构化分段（借鉴 Kode 8 段），落 `internal/runtime/context/templates/compact_prompt.md`，可被 Agent App / Hook 覆盖 `compact_instructions`：
  ```text
  技术上下文 / 任务目标 / 关键改动 / 调试与决策 / 当前状态 / 待办 / 用户偏好 / 关键结论
  ```
- **压缩用模型**：复用**已存在**的 `config.yaml → llm.router.summarization`（默认 `fast`）选择便宜模型（借鉴 Kode compact 指针）。
- **回填（替换而非丢弃）**：
  - 内存：`history = [摘要消息] + [保留的最近消息]`；摘要作为一条带标记的消息（`role=user`，`<conversation-summary>` 包裹，借鉴 codex 把摘要作 user 消息以便模型当"既有背景"）。
  - 持久层：写一条 `summary` 记录 + 更新 leaf 指针；**原始 JSONL 不删除**（可追溯、可 fork 回旧点）。
- **压缩后处理（可选）**：关键工件恢复——把最近读过的少量文件/工件重新注入（借鉴 Kode fileRecovery，受独立小预算约束），避免摘要丢失代码上下文。
- **长期记忆沉淀钩子**：压缩时"即将被摘要吞并的原始历史"是长期记忆抽取的天然输入点（§七.3）。

#### 压缩编排接口

```go
// Compactor 编排两级压缩，落 internal/runtime/context（内核，产品无关）。
type Compactor interface {
    // MaybeMicroCompact 在每轮 LLM 调用前尝试卸载大工具结果（就地、无 LLM）。
    MaybeMicroCompact(ctx context.Context, rc *runtime.RunContext) (Compaction, error)
    // MaybeAutoCompact 在使用率超阈时用 LLM 生成摘要并替换历史。
    MaybeAutoCompact(ctx context.Context, rc *runtime.RunContext, ref SessionRef) (Compaction, error)
}

type Compaction struct {
    Triggered   bool
    Kind        string // "micro" | "auto" | "none"
    TokensSaved int
    SummaryID   string // auto 时的 summary 记录 ID
}
```

- **react_loop 改造点**：`react_loop.go:284` 每轮 `StreamGenerate` 前调 `MaybeMicroCompact`；当估算使用率超阈时调 `MaybeAutoCompact`。压缩期间持有会话锁，置会话 `compacting`。

---

## 七、长期记忆：如何形成、存储与检索

长期记忆是本方案最大的子课题。核心纪律（对齐 `docs/agent loop设计.md §11.6`）：**框架不自动、无审批地把 Run 结论写进长期记忆**——"记什么"是业务决策；框架只提供**抽取器骨架、可配置提示词/结构、存储契约、读取注入**。

### 7.1 三种形成来源

| 来源 | 触发 | 内容 | 审批 |
| --- | --- | --- | --- |
| **用户显式** | `#记住...` 命令 / "记住这个偏好"工具（借鉴 Kode hashCommand） | 用户原话/偏好/项目约定 | 无需（用户主动） |
| **抽取器沉淀** | 会话压缩时 / Run 结束时 / 会话归档时（业务选择） | 从对话中提炼的经验、结论、失败教训 | 可配"直接入库"或"入待审队列" |
| **项目文档** | 目录链发现 `AGENTS.md`（借鉴 codex/Kode，与提示词分层 `ProjectDocInjector` 共用） | 项目级稳定约定 | 文件即真相，可编辑 |

### 7.2 两阶段抽取（借鉴 codex memories Phase1/Phase2）

```text
Phase 1 抽取（per-Run / per-压缩批次，异步）
  输入：被摘要吞并的原始历史 或 整个 Run 的消息链
  过程：用 extraction_prompt 让 LLM 产出候选记忆条目（结构化 JSON）
  输出：raw LongTermEntry[]（带 source_run_id / source_message_id 证据）

Phase 2 合并（周期性 / 阈值触发，异步）
  过程：与现有同 scope 记忆做相似度比较 → merge / supersede / 丢弃低价值
  输出：去重、更新 importance/confidence 后的稳定记忆集
```

- **异步**：抽取与合并都走后台（对齐 §11.6 方式 B），不阻塞对话响应。
- **不同项目沉淀不同内容 → 可自定义抽取**：

```go
// MemoryExtractor 业务可自定义的抽取器（框架提供默认实现 + 骨架）。
type MemoryExtractor interface {
    // Extract 从一段消息中抽取候选长期记忆。
    Extract(ctx context.Context, in ExtractInput) ([]*domain.LongTermEntry, error)
}

// ExtractSpec 声明式配置：不同 Agent App / 项目可各自定义。
type ExtractSpec struct {
    Enabled       bool
    Trigger       string            // "on_compact" | "on_run_complete" | "manual"
    Prompt        string            // 自定义抽取提示词（决定"抽什么"）
    Schema        map[string]any    // 自定义 custom_data 结构（JSON Schema，决定"存成什么结构"）
    Scope         MemoryScope       // 沉淀到 user / agent_instance / workspace / project
    ReviewMode    string            // "auto" | "review_queue"
    MaxPerBatch   int               // 单批最多抽取条数（防噪声）
}
```

- **默认实现**：`LLMMemoryExtractor` 用内置通用抽取提示词（"提炼用户稳定偏好、项目约定、关键结论、失败教训，忽略一次性事实"）。
- **自定义**：`ExtractSpec.Prompt` + `Schema` 允许"客服项目沉淀常见问题解法"、"编码项目沉淀仓库约定与踩坑"，各存各的结构化 `custom_data`。
- **配置来源**：`app.yaml` 的 `memory.extract` 节 → `ExtractSpec`（与 Agent App `AgentSpec` 对齐，见 §九）。

### 7.3 检索与注入

```text
Run 启动：LongTermMemory.Search(MemoryQuery{
    Query: user_input, Scopes: [user, agent_instance, project],
    TopK: memory.inject_top_k(默认 5), MinConfidence: 0.5,
    Filters: {memory_type, sensitivity ≤ 允许级别},
})  → 注入 system 的 <long_term_memories> 块（受 §八 预算 clamp）
```

- **检索退化链**：向量库(语义) → 无向量则关键词/标签/importance 排序（文件后端）。同一 `Search` 契约，后端能力不同。
- **敏感度过滤**：`secret/pii` 默认不注入（`docs/agent loop设计.md §11.9`）。
- **可选检索工具**：Enterprise 可暴露 `memory_search`/`memory_read` 工具让模型主动查（借鉴 codex ext/memories tools），而非只在启动时静态注入。

### 7.4 数据模型（落 `internal/domain`）

```go
// LongTermEntry 长期记忆条目（对齐 docs/agent loop设计.md §11.4，补齐字段）。
type LongTermEntry struct {
    ID            string
    TenantID      string
    ScopeType     string   // user / agent_instance / workspace / project
    ScopeID       string
    MemoryType    string   // episodic / semantic / procedural / negative
    Content       string
    CustomData    map[string]any // 业务自定义结构（由 ExtractSpec.Schema 约束）
    Embedding     []float32      // 向量后端写入；文件后端为空
    Importance    float64  // 0~1
    Confidence    float64  // 0~1
    SensitivityLevel string // public/internal/confidential/secret/pii
    DecayPolicy   string   // none/time_decay/access_decay/custom
    Tags          []string
    SourceRunID   string
    SourceMessageID string
    SupersedesID  string
    LastAccessedAt time.Time
    ExpiredAt     *time.Time
    domain.RuntimeAudit
}

// UserProfile 用户画像（对齐 §11.8）。
type UserProfile struct {
    TenantID     string
    UserID       string
    BuiltinFields map[string]any // locale/communication_style/tool_preferences/risk_preference/memory_opt_in...
    CustomFields  map[string]any
    Evidence      map[string]FieldEvidence // 字段来源证据
    Confidence    map[string]float64
    Visibility    map[string]string        // user_visible/internal/system_only
    domain.RuntimeAudit
}
```

> 现有 `domain.MemoryEntry`（`internal/domain/memory.go`）字段较少，将**演进为** `LongTermEntry`（补 scope/memory_type/confidence/sensitivity/decay），旧结构体清理。

### 7.5 记忆更新与衰退（对齐 §11.9）

- 更新策略：`append / merge / supersede / correct(用户纠正优先) / delete`。
- 衰退评分：`score = importance × confidence × recency × access_factor`；低分降权不物理删除；用户显式保存/合规类不自动衰退。

---

## 八、上下文窗口预算与分配（用户重点问题）

### 8.1 模型配置：新增最大上下文字段

私有化部署模型无法自动探测窗口，必须可配置。在 `LLMModelConfig` / `ResolvedLLMConfig`（`internal/platform/config/config.go`）**新增**：

```go
type LLMModelConfig struct {
    // ... 现有字段 ...
    MaxTokens            int      `mapstructure:"max_tokens"`             // 现有：单次输出上限
    ContextWindow        int      `mapstructure:"context_window"`         // 新增：模型总上下文窗口（token）
    EffectiveContextRatio *float64 `mapstructure:"effective_context_ratio"` // 新增：有效比例，默认 0.92（借鉴 codex 95%）
    OutputReserveTokens  int      `mapstructure:"output_reserve_tokens"`  // 新增：输出预留下限（0 时用 MaxTokens）
    TokenizerHint        string   `mapstructure:"tokenizer_hint"`         // 新增：估算器提示（heuristic/tiktoken/cl100k...）
}
```

`config.yaml` 示例：

```yaml
llm:
  models:
    default:
      model: gpt-4o
      max_tokens: 4096            # 输出上限
      context_window: 128000      # 新增：总窗口
      effective_context_ratio: 0.92
    local:                        # 私有化部署，窗口小且需手动填
      model: qwen2.5-32b
      max_tokens: 4096
      context_window: 32768       # 必填，无法探测
      effective_context_ratio: 0.90
  router:
    summarization: fast           # 已存在：复用为压缩模型
```

- `ContextWindow` 缺省的兜底：按 provider/model 名匹配内置默认表（可选），再兜底一个保守值（如 8192）并告警。

### 8.2 预算计算公式

分两类段：**刚性段**（必须完整放入，否则任务无法进行）与**弹性段**（可伸缩，按预算取舍）。

```text
# 第 1 步：可用输入预算
usable          = context_window × effective_context_ratio        # 扣协议/tokenizer 误差头
output_reserve  = clamp(output_reserve_tokens or max_tokens, MIN_OUT, MAX_OUT)  # 为模型回复预留
input_budget    = usable − output_reserve                          # 全部输入段共享的总预算

# 第 2 步：先满足刚性段（按实际测量值扣除）
# 注意：system_prompt 这里只含"稳定 prompt 块"（L0~L2 persona/instructions/安全/工具协议），
#       不含 LTM/摘要/历史等动态内容——后者是第 3 步的弹性段（见 8.4 分工说明）。
rigid     = tok(stable_system_blocks) + tok(tools_schema) + tok(current_user)
remaining = input_budget − rigid
若 remaining < 0 → 降级（见 8.5）

# 第 3 步：先扣安全缓冲，再把可分配额度按"目标比例 + 区间 clamp + 额度回流"分给弹性段
safety_buffer = remaining × buffer_ratio            # 默认 5%，吸收估算误差，永不分配
distributable = remaining − safety_buffer
弹性段 = { history(含本 Run 工作观察), summary, long_term_memory }   # 三段权重之和 = 100%
for each seg:
    target_i = distributable × weight_i
    alloc_i  = clamp(target_i, min_i × distributable, max_i × distributable)  # 区间限制
# 回流：clamp 后（命中 max 或实际内容不足）会有结余；
#       结余按"未达 max 的可扩张段"权重再分配，优先补给 history（最近最相关），迭代至收敛
reflow(结余) → 优先 history → 再 summary → 再 ltm
```

**默认权重与区间**（占 `distributable` 的比例，三段权重之和 = 100%，全部可配）：

| 弹性段 | 目标权重 | 最小 | 最大 | 说明 |
| --- | --- | --- | --- | --- |
| `history`（含工作观察） | 60% | 20% | 90% | 最近对话最相关，回流优先补给它 |
| `summary` | 15% | 0% | 30% | 有摘要才占用；无摘要额度回流 |
| `long_term_memory` | 25% | 0% | 40% | 检索命中才占用；受 TopK 与 confidence 双限 |

> `safety_buffer`（`remaining × 5%`，可配 5%~10%）在分配前独立扣除，**不参与**三段权重，故三段权重严格求和为 100%，避免"权重不足 100% 导致预算浪费"。
>
> **关键**：这正是用户要的"不严格按比例、某部分少了另一部分可以多些、但有区间限制"——通过 **clamp（区间）+ reflow（回流）** 实现。例如某会话还没有长期记忆和摘要，`history` 可回流吃到 90% 上限；反之长期记忆很多时，`ltm` 最多到 40%，多出的挤不进去（保证 history 不被饿死）。

### 8.3 历史记录的取用规则（条数 + 长度 + 占比）

`ShortTermMemory.GetRecent` 在 `history` 段预算内从新到旧取：

- **条数上限**：`max_history_turns`（默认 40 轮），防止极多短消息塞爆。
- **token 预算**：`history` 段分配到的 token（§8.2）。
- **单条最大长度**：单条消息超 `per_message_max_tokens`（默认 4k）时，先走 micro-compact 卸载（§六.4 L1），而非整条丢弃。
- **取用顺序**：从最新消息回溯累加，直到触达条数或 token 上限；**保留完整 tool 链的成对性**（assistant.tool_calls 与其 tool result 不拆散）。
- **首条保护**：会话首条 user 消息（任务定义）在有摘要时由摘要代表，无摘要时优先保留（借鉴 codex/§4.8）。

### 8.4 上下文装配顺序（ContextAssembler）

```text
最终 messages（对齐 docs/agent loop设计.md §4.8 与 §11.7）：
  [1] system      = PromptComposer 产出（含 long_term_memories / user_profile 块，见提示词分层方案）
  [2] history     = summary 消息（若有）+ GetRecent(history 段预算) 裁剪后的历史
  [3] working_obs = 本 Run 工作观察（tool 结果，micro-compact 后）
  [4] user        = 当前用户输入 + user_prefix reminder（提示词分层 L4）
```

> **分工（消除两方案职责重叠）**：
> - `ContextBudgetPlanner`（本方案）**先**算出每个弹性段（LTM/摘要/历史）各能占多少 token。
> - `PromptComposer`（提示词分层方案）负责 system 段的分层 Block 拼装；LTM 块、画像块虽在文本上位于 system 消息内，但其**内容量**由 Planner 下发的 LTM 预算约束（Composer 只按给定预算渲染，不自行决定塞多少）。
> - `ContextAssembler`（本方案）负责 history/摘要/工作观察的裁剪与最终 messages 拼装。
> - 因此 §8.2 把"稳定 system 块"算作刚性、把 LTM/摘要/历史算作弹性，与"它们最终渲染在哪条消息里"是两个正交维度：**预算归 Planner，渲染位置归 Composer/Assembler**。
>
> **命名对齐**：本方案的 `ContextAssembler` + `ContextBudgetPlanner` 合起来即 `docs/agent loop设计.md §4.8/§11.7` 所称的 `ContextBuilder`；本方案将其拆为"预算"与"装配"两个职责更单一的组件。

### 8.5 溢出降级（刚性段都放不下时）

按顺序尝试，每步有 trace/audit：

1. 强制 auto-compact（把历史压到最小保留集）。
2. 裁剪工具 schema（仅保留本轮可能用到的工具，借鉴按需暴露）。
3. 缩减长期记忆/摘要到各自 min。
4. 当前 user 输入过长 → 提示用户拆分，或对输入本身做摘要（谨慎，保留原文引用）。
5. 仍不行 → 明确报错（不静默截断关键信息，满足 §2.3 不变量 3）。

### 8.6 Token 估算（TokenEstimator 端口）

```go
type TokenEstimator interface {
    Estimate(text string) int
    EstimateMessages(msgs []*domain.Message) int
}
```

- 默认 `HeuristicEstimator`：借鉴 codex `bytes/4` + Kode 开销系数；**CJK 加权**（中文字符按 ~0.6~1 token/字估算，比纯 bytes/4 更准）+ 每消息固定 overhead（role/分隔符）。
- 可选 `TiktokenEstimator`（Enterprise 精确计费）、`ProviderUsageEstimator`（用 API 返回的 usage 校准，作权威计费；估算仅用于"装之前"的预算决策）。
- **接线**：`RunContext.AddTokens`（现有但未调用）在每次 LLM 调用后用 provider usage 更新；预算决策用 estimator 的估算值（借鉴 Kode/codex 双轨）。

---

## 九、多智能体兼容（对齐 Agent App 三层模型）

`docs/Agent App ...md` 定义了 L1（App 内委派子智能体）/ L2（App 内 Workflow 编排）/ L3（App 间 CollaborationSpace）。会话与记忆**当前阶段只需预留维度、正确隔离，不实现编排**。

### 9.1 Session 模型扩展

```go
// domain.Session 扩展（现状仅 6 字段）
type Session struct {
    ID        string
    TenantID  string
    UserID    string
    AgentID   string   // 现有
    AppID     string   // 新增：Agent App（哪个应用发起的会话）
    Title     string
    // 上下文/压缩元数据
    Status       string // active/idle/compacting/archived
    SummaryLeaf  string // 压缩边界指针（恢复用）
    TotalTokens  int64  // 累计 token（计费/展示）
    ContextStrategy string // sliding_window/summary/selective（§4.8，Agent 可配）
    // 时间
    CreatedAt    time.Time
    LastActiveAt time.Time
}
```

### 9.2 隔离维度：`SessionRef` 与 `MemoryScope`

- **会话内多智能体**：一个 Session 可涉及主 Agent + 子智能体（L1）或 Workflow 多角色（L2）。消息 envelope 记 `agent_instance_id`；子智能体消息走**旁链 JSONL**（`{sessionID}/subagents/agent-{id}.jsonl`，借鉴 Kode），主流只保留委派摘要，避免子链爆主上下文（对齐 `docs/子智能体设计.md` 预算熔断）。
- **记忆隔离**：`MemoryScope` 支持 `user / agent_instance / workspace / project`。不同 Agent 实例默认记忆隔离（`scope=agent_instance`）；项目级经验用 `scope=project` 跨 Agent 共享（需业务显式声明）。
- **L3 协作空间**：多个独立 App 成员的会话/记忆由 CollaborationSpace（`internal/runtime/multiagent`，Phase 3+）统筹；本方案只保证 `SessionRef`/`LongTermEntry` 带 `app_id`/`scope` 字段，L3 落地时无需迁移数据模型。

### 9.3 兼容原则

- 单 Agent（当前默认 `code` 应用）：`app_id`/`agent_instance_id` 可为空或取默认，行为等价现状。
- 字段先行、编排后置：数据模型一次到位，编排逻辑分阶段接入，避免未来改表/改协议。

---

## 十、三产品统一与差异（存储与检索）

内核（预算算法、装配顺序、压缩编排、记忆注入顺序、四层接口）**三端同源，零 `if product`**；差异只经**存储/检索/token 后端**注入（对齐 `docs/产品分发架构设计.md` 与提示词分层方案 §九）。

| 维度 | 契约（内核依赖） | CLI | Desktop | Enterprise |
| --- | --- | --- | --- | --- |
| 短期记忆存储 | `ShortTermMemory` | 本地 JSONL（`~/.genesis-agent/sessions/`） | 本地 JSONL/SQLite + 可视化浏览 | PostgreSQL `agent_message` / Redis 热缓存，租户隔离 |
| 长期记忆存储 | `LongTermMemory` | 本地文件 + `AGENTS.md`（关键词/标签检索） | 本地 SQLite + 可视化编辑记忆/画像 | PostgreSQL+pgvector / Qdrant（语义检索）+ RBAC |
| 用户画像 | `UserProfileStore` | 本地 JSON | 本地 JSON + UI 编辑 | DB / CRM / 画像服务 |
| Token 估算 | `TokenEstimator` | Heuristic（CJK 加权） | Heuristic | Tiktoken 精确 / Provider usage 校准计费 |
| 抽取器 | `MemoryExtractor` | 默认 LLM 抽取，本地入库 | 同 CLI + UI 审核队列 | 自定义 ExtractSpec + 审核流 + 审计 |
| 会话存储路径 | `SessionStore` | 宿主机绝对路径 | 宿主机 | **workspace-relative / sandbox path**（禁宿主机绝对路径，对齐 AGENTS.md 沙箱边界） |
| 上下文使用率展示 | progress 事件 | TUI TokenWarning（借鉴 Kode） | 图形化余量条 | Web 面板 + 审计 |

- **统一**：四层记忆契约、预算公式、压缩机制、恢复语义、注入顺序、SourceChain 可解释性。
- **各自独立**：存储后端、检索能力（关键词 vs 语义）、画像来源、估算精度、可视化/审计深度。
- **Enterprise 关键约束**：会话/记忆中不得落宿主机绝对路径；`secret/pii` 记忆默认不注入且需 RBAC 才可查看；多租户查询强制带 `tenant_id`（对齐 AGENTS.md）。

---

## 十一、目录与边界（遵循 `docs/项目目录与边界说明.md`）

```text
internal/domain/
  session.go            # 扩展：Session（+status/summary_leaf/total_tokens/app_id...）
  message.go            # 现有：Message（已含完整链字段，无需改）
  memory.go             # 演进：MemoryEntry → LongTermEntry（补 scope/type/confidence...）
  user_profile.go       # 新增：UserProfile / ProfileField
  session_summary.go    # 新增：SessionSummary（摘要 + leaf 指针）

internal/capabilities/memory/
  contract/interface.go # 扩展：ShortTermMemory / LongTermMemory / UserProfileStore / MemoryExtractor / SessionStore
  adapter/inmemory/     # 现有：升级实现 ShortTermMemory
  adapter/file/         # 新增：FileShortTermMemory(JSONL) / FileLongTermMemory / FileUserProfileStore
  adapter/db/           # 后续：PG 实现（Enterprise）
  adapter/vector/       # 后续：pgvector/Qdrant（Enterprise 语义检索）
  service/              # 抽取器默认实现 LLMMemoryExtractor、Consolidator（业务可覆盖）

internal/runtime/context/          # 内核：预算 + 装配 + 压缩（产品无关）
  budget.go             # 新增：ContextBudgetPlanner（§八公式）
  assembler.go          # 新增：ContextAssembler（§8.4 装配顺序）
  compactor.go          # 新增：Compactor（§六 两级压缩编排）
  estimator.go          # 新增：TokenEstimator 端口 + HeuristicEstimator
  templates/compact_prompt.md  # 新增：结构化摘要模板（可被 App/Hook 覆盖）

internal/runtime/context.go        # 现有 RunContext：接线 AddTokens、承载 WorkingMemory

internal/platform/config/config.go # 扩展：LLMModelConfig 增 context_window 等字段

products/<product>/bootstrap/       # 注入 ShortTermMemory/LongTermMemory/UserProfileStore/TokenEstimator/SessionStore 后端
shared/local/memory/                # CLI/Desktop 本地文件后端实现（若需落地）
```

边界约束：

- **依赖方向（精确表述）**：`internal/runtime/context` 可 import 能力域的**契约端口包**（`internal/capabilities/memory/contract` 的 `ShortTermMemory`/`SessionRef` 等接口与值类型，以及 `llm/contract`）——这与现有 `runtime/strategy/react` 已 import `memory.ShortTermStore`、`llm.ChatModel` 的约定一致；但**不得** import 能力域的 `adapter/*` 具体实现、`products/*`、Docker/DB/向量库 SDK。
- 记忆/会话的**具体存储后端**在 `capabilities/memory/adapter/*` 与 `products/*/bootstrap` 注入。
- 合法方向：`capabilities/memory -> domain`；`runtime/context -> domain + capabilities/*/contract 端口`。被禁止的是 `runtime -> capabilities/*/adapter`（依赖实现）与 `runtime -> products`。
- 压缩摘要模板常量放 `internal/runtime/context/templates`（产品无关，三端唯一真源）。

---

## 十二、分期落地

| 阶段 | 内容 | 验收 |
| --- | --- | --- |
| **Phase 1**（基础正确性） | ① `Message` 完整链持久化（修正只存 user+final）；② `ShortTermMemory` 接口扩展 + `FileShortTermMemory`(JSONL) + 恢复；③ `config` 增 `context_window` 等字段并接线 `max_tokens` 到 eino；④ `TokenEstimator`(Heuristic CJK) + `RunContext.AddTokens` 接线；⑤ `ContextBudgetPlanner` + `ContextAssembler`（§八公式，sliding_window 策略） | 长会话不再溢出报错；重启后会话可恢复；上下文永不超有效窗口；`agent current` 可显示各段预算 |
| **Phase 2**（压缩与记忆） | ① 两级 `Compactor`（micro + auto，复用 `router.summarization`）+ 摘要回填/持久化；② `LongTermMemory` 接口 + `FileLongTermMemory`（关键词）+ 启动检索注入；③ `UserProfileStore` + `FileUserProfileStore`；④ `MemoryExtractor` 默认实现 + `ExtractSpec` 配置 + 异步两阶段；⑤ 用户 `#记住` 入口 | 使用率超阈自动压缩且信息不丢；跨会话记忆检索注入；画像可编辑；不同 App 可配抽取 |
| **Phase 3**（生产化与优化） | ① Enterprise DB/向量后端（pgvector/Qdrant）+ 语义检索 + RBAC/审计；② 上下文差量注入（借鉴 codex reference_context_item）+ 稳定前缀缓存；③ selective 策略（RAG 式历史召回）；④ 多智能体旁链/协作空间记忆隔离落地；⑤ 记忆衰退/合并后台任务、Skill Candidate 沉淀（§11.10） | 企业级隔离/审计；差量省 token；长期记忆语义召回；多 Agent 记忆正确隔离 |

> 每阶段结束删除被替换的临时实现（对齐 AGENTS.md"开发阶段不兼容旧代码"）：Phase 1 后删旧 `ShortTermStore` 3 方法接口与 `react_loop` 只存两条的逻辑；Phase 2 后 `domain.MemoryEntry` 并入 `LongTermEntry`。

---

## 十三、关键决策摘要

| 决策点 | 结论 |
| --- | --- |
| 记忆分层 | 四层：工作(RunContext) / 短期(Session 对话) / 长期(跨会话经验) / 画像；生命周期与存储各异 |
| 消息持久化 | 存**完整消息链**（含 tool_calls/tool result/reasoning），追加式 JSONL + 摘要 leaf 指针；修正现状只存 user+final |
| 会话恢复 | 按 summary leaf 截断重建（resume）；支持 fork；原始历史不删可追溯 |
| 压缩机制 | 两级：micro-compact(工具结果外置，无 LLM) + auto-compact(LLM 结构化摘要)；摘要"替换非丢弃" |
| 压缩触发 | 使用率 ≥ compact_ratio(默认 0.85) 且轮数 ≥ min_turns；micro 在 warn_ratio(0.75) |
| 保留/压缩多少 | 保留最近 keep_recent_turns(默认 6~10) 且不超 keep_recent_token_budget；其余全部摘要（轮数+token 双约束） |
| 模型最大上下文 | 配置新增 `context_window` 字段（私有化模型必填）+ `effective_context_ratio`(默认 0.92) |
| 预算公式 | usable=窗口×有效比例；input_budget=usable−输出预留；刚性段优先，弹性段按权重 clamp + 回流 + safety_buffer |
| 比例弹性 | "目标权重 + [min,max] 区间 clamp + 结余回流(优先 history)"——不严格按比例但有区间限制 |
| 历史取用 | 条数上限 + token 预算 + 单条上限(超则 micro-compact) + tool 链成对保留 + 首条保护 |
| 长期记忆形成 | 三来源(用户显式/抽取器/项目文档)；两阶段(抽取→合并) 异步；框架不自动无审批写入 |
| 自定义抽取 | `ExtractSpec`(prompt+schema+scope+review) 支持不同项目沉淀不同结构化内容 |
| Token 估算 | `TokenEstimator` 端口：默认 Heuristic(CJK 加权)，可选 tiktoken/provider usage 双轨 |
| 三产品 | 内核同源；存储/检索/token/画像后端经端口注入差异化；Enterprise 禁宿主机绝对路径 + RBAC + pii 不注入 |
| 多智能体 | Session/Message/Memory 预留 app_id/agent_instance_id/scope；子智能体旁链；字段先行编排后置 |
| 与提示词分层 | PromptComposer 管 system 段分层块；本方案 ContextAssembler 管 history/memory/预算；两方案对接不重叠 |
| 复用现有路由 | 压缩模型复用已存在的 `config.yaml llm.router.summarization` |

---

## 十四、残留风险（不阻塞 Phase 1）

1. **摘要质量与信息丢失**：结构化模板 + 关键工件恢复缓解，但仍需在真实长会话上验证；建议保留原始 JSONL 以便回退/评估。
2. **Token 估算误差**：Heuristic 对代码/多语言/工具 JSON 误差较大，靠 safety_buffer + provider usage 校准兜底；Enterprise 上 tiktoken 提精度。
3. **CJK 估算系数**需按实际模型 tokenizer 标定（不同模型中文分词差异大）。
4. **长期记忆污染/隐私**：抽取默认可走 review_queue；`secret/pii` 分级与 RBAC 需与统一权限治理（`docs/统一配置权限与审批治理设计.md`）对齐后定稿。
5. **压缩与并发 Run**：同会话并发 Run 需会话锁 + `compacting` 状态防脏读；多副本部署下需分布式锁（Enterprise Phase 3）。
6. **差量注入**（Phase 3）与摘要/历史序列的交互需验证不破坏对话连续性（借鉴 codex 需谨慎处理 compaction 后强制全量 reinject）。

---

## 附录 A：Kode-CLI 相关源码文件清单（相对 `D:\workspace\go\go-project\Kode-CLI` 根）

**会话与消息**

| 文件 | 职责 |
| --- | --- |
| `packages/core/src/messages.ts` | 消息全局 getter/setter |
| `packages/core/src/engine/pipeline/types.ts` | `UserMessage/AssistantMessage/ProgressMessage` 类型 |
| `packages/core/src/engine/orchestrator.ts` | `query()` 持久化 + pipeline 递归 |
| `packages/core/src/engine/message-pipeline.ts` | per-turn 组装、压缩检查时序 |
| `packages/core/src/engine/turn.ts` | `runTurn` 入口 |

**协议/持久化（JSONL + resume）**

| 文件 | 职责 |
| --- | --- |
| `packages/protocol/src/sessionJsonl.ts` | JSONL 记录类型（user/assistant/summary/tag/file-history） |
| `packages/protocol/src/utils/kodeAgentSessionLog.ts` | `getSessionLogFilePath` 路径约定 |
| `packages/protocol/src/utils/kodeAgentSessionLoad.ts` | `loadForResume`（按 leafUuid 截断）`:370` |
| `packages/protocol/src/utils/kodeAgentSessionResume.ts` | resume 逻辑 |
| `packages/protocol/src/utils/kodeAgentSessionForkInfo.ts` | fork |

**Token/上下文预算**

| 文件 | 职责 |
| --- | --- |
| `packages/core/src/utils/tokens.ts` | `countTokens`(API usage) / `estimateTokens`(char/4 + 系数) |
| `packages/core/src/utils/autoCompactThreshold.ts` | reserve(10% cap 20k)/margin(13k)/warning 阈值 `:8` |
| `packages/core/src/utils/contextWindowPercentages.ts` | UI 使用率百分比 |
| `packages/core/src/ai/llm/maxTokens.ts` | 输出 max_tokens 决议 |
| `packages/core/src/model/manager.ts` | `main/compact/task/quick` 模型指针 |
| `packages/config/src/schema.ts` | `ModelProfile.contextLength/maxTokens` `:181` |

**压缩/摘要**

| 文件 | 职责 |
| --- | --- |
| `packages/core/src/utils/microCompactCore.ts` | L1 工具结果卸载（KEEP_LAST_TOOL_USES=3） |
| `packages/core/src/utils/autoCompactCore.ts` | L2 LLM 摘要（COMPRESSION_PROMPT_BASE 8 段、回填 `:320`） |
| `packages/core/src/utils/compactionSnapshots.ts` | 摘要附加快照（任务列表等） |
| `packages/core/src/utils/toolResultPersistence.ts` | 工具结果外置到磁盘 |
| `packages/core/src/utils/fileRecoveryCore.ts` | 压缩后恢复最近文件 |
| `packages/core/src/services/fileFreshness.ts` | 文件新鲜度跟踪 |
| `apps/cli/src/commands/builtin/compact.ts` | 手动 `/compact` |

**记忆/项目上下文**

| 文件 | 职责 |
| --- | --- |
| `packages/core/src/context/index.ts` | `getContext()` 会话级 memoize |
| `packages/core/src/utils/projectInstructions.ts` | `AGENTS.md` 目录链发现（override/32KB） |
| `packages/core/src/utils/hashCommand.ts` | `#` 写入 AGENTS.md |
| `packages/core/src/ai/llm/kodeContext.ts` | AGENTS.md 缓存注入 |
| `packages/core/src/constants/product.ts` | `PROJECT_FILE=AGENTS.md` |

**UI**

| 文件 | 职责 |
| --- | --- |
| `apps/cli/src/ui/components/TokenWarning.tsx` | 上下文低余量提示 |
| `apps/cli/src/ui/components/PromptInput/PromptInput.tsx` | 使用率百分比展示 |

> 建议阅读顺序：`autoCompactThreshold.ts` → `tokens.ts` → `microCompactCore.ts` → `autoCompactCore.ts` → `kodeAgentSessionLoad.ts` → `projectInstructions.ts`。

---

## 附录 B：codex 相关源码文件清单（相对 `D:\workspace\go\go-project\codex` 根）

**会话 / 历史 / Rollout**

| 文件 | 关键符号 |
| --- | --- |
| `codex-rs/core/src/context_manager/history.rs` | `ContextManager`(items Vec `:36`)、`get_total_token_usage`(`:328`)、`estimate_token_count` |
| `codex-rs/core/src/context_manager/updates.rs` | 上下文 diff / developer-user message 构建 |
| `codex-rs/core/src/state/session.rs` | `SessionState`（history + token + auto_compact_window） |
| `codex-rs/core/src/state/auto_compact_window.rs` | 压缩窗口链 / prefill 基线 |
| `codex-rs/core/src/session/mod.rs` | `record_conversation_items`/`replace_compacted_history`/`build_initial_context` |
| `codex-rs/core/src/session/context_window.rs` | `context_window_token_status`（使用率/阈值状态机） |
| `codex-rs/core/src/session/turn_context.rs` | `model_context_window`(有效窗口=窗口×95% `:188`) |
| `codex-rs/core/src/session/token_budget.rs` | token budget 提醒 |
| `codex-rs/core/src/session/rollout_reconstruction.rs` | resume 重建 |
| `codex-rs/rollout/src/recorder.rs` | JSONL rollout 写入 |
| `codex-rs/protocol/src/protocol.rs` | `RolloutItem` 枚举(`:3153`)、`TokenUsageInfo` |

**压缩**

| 文件 | 职责 |
| --- | --- |
| `codex-rs/core/src/compact.rs` | Inline 摘要（`build_compacted_history` `:585`、`COMPACT_USER_MESSAGE_MAX_TOKENS`） |
| `codex-rs/core/src/compact_remote.rs` / `compact_remote_v2.rs` | 服务端压缩 |
| `codex-rs/core/src/compact_token_budget.rs` | token budget 换窗口（不摘要） |
| `codex-rs/prompts/templates/compact/prompt.md` | 压缩指令模板 |
| `codex-rs/prompts/templates/compact/summary_prefix.md` | 摘要前缀标记 |
| `codex-rs/protocol/src/compacted_item.rs` | `CompactedItem.replacement_history` |

**记忆 / AGENTS.md**

| 文件 | 职责 |
| --- | --- |
| `codex-rs/core/src/agents_md.rs` | 项目文档发现/读取/合并 |
| `codex-rs/core/src/context/world_state/agents_md.rs` | WorldState AGENTS 段 |
| `codex-rs/memories/write/src/phase1.rs` | Phase1 抽取 |
| `codex-rs/memories/write/src/phase2.rs` | Phase2 合并 |
| `codex-rs/memories/write/templates/memories/stage_one_system.md` | 抽取系统提示 |
| `codex-rs/memories/write/templates/memories/consolidation.md` | 合并提示 |
| `codex-rs/ext/memories/src/extension.rs` | 读时注入 memory_summary |
| `codex-rs/ext/memories/templates/memories/read_path.md` | 读路径注入模板 |
| `codex-rs/state/src/runtime/memories.rs` | 记忆 state 模型 |

**模型元数据 / Token / 提示词**

| 文件 | 职责 |
| --- | --- |
| `codex-rs/models-manager/models.json` | `context_window`/`auto_compact_token_limit`/`effective_context_window_percent` |
| `codex-rs/protocol/src/openai_models.rs` | `ModelInfo`/`resolved_context_window`/`auto_compact_token_limit`(`:441`) |
| `codex-rs/models-manager/src/config.rs` | 用户覆盖 `model_context_window`/`tool_output_token_limit` |
| `codex-rs/utils/string/src/truncate.rs` | `approx_token_count`(bytes/4 `:71`) |
| `codex-rs/core/src/client_common.rs` | `Prompt`（base_instructions 与 input 分离 `:17`） |
| `codex-rs/core/src/config/mod.rs` | `model_auto_compact_token_limit`/`compact_prompt` 覆盖 |

> 建议阅读顺序：`session/context_window.rs` → `turn_context.rs`(`:188`) → `compact.rs`(`:585`) → `context_manager/history.rs` → `memories/write/src/phase1.rs`/`phase2.rs` → `models.json` + `openai_models.rs`(`:441`)。

---

## 附录 C：本项目改造锚点速查

| 改造项 | 锚点文件:行 |
| --- | --- |
| 消息只存两条 → 存完整链 | `internal/runtime/strategy/react/react_loop.go:413` |
| 全量历史无预算 → 预算裁剪 | `internal/runtime/strategy/react/react_loop.go:171` |
| ShortTermStore 扩展 | `internal/capabilities/memory/contract/interface.go:15` |
| InMemory 升级 | `internal/capabilities/memory/adapter/inmemory/inmemory.go` |
| Session 模型扩展 | `internal/domain/session.go` |
| MemoryEntry → LongTermEntry | `internal/domain/memory.go` |
| config 增 context_window | `internal/platform/config/config.go:149`(LLMModelConfig)、`:165`(ResolvedLLMConfig) |
| max_tokens 接线 eino | `internal/capabilities/llm/adapter/eino/factory.go` |
| AddTokens 接线 | `internal/runtime/context.go` |
| 压缩模型路由复用 | `configs/config.yaml`（`llm.router.summarization`） |

