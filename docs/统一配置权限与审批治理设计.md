# 统一配置、权限与审批治理设计

> 本文档设计 Genesis Agent 的配置体系、权限策略、审批授权与运行时配置边界。它覆盖 CLI、Desktop、Enterprise 三类产品，但不强行要求所有产品使用同一种交互方式；统一的是配置模型、策略决策和审计语义，差异留在产品 bootstrap 和 requester/adapter 中。

## 一、问题与结论

当前 `configs/config.yaml`、`configs/llm.yaml`、`configs/mcp.yaml` 已覆盖 LLM、HTTP Client、Secrets、Agent、Log、Server、MCP 等基础配置，但文件系统、网络、命令执行、沙箱、Skill、连接/密钥、审批授权等高风险能力仍需统一配置面。代码中已经出现了几个能力雏形：

- `internal/capabilities/approval/model` 已定义 `allow / ask / deny`、`once / turn / session / project / tenant / global` 授权范围。
- `internal/capabilities/filesystem/permission` 已有运行时文件授权缓存和文件风险上下文。
- CLI bootstrap 已有 `quiet` 模式、终端审批 requester、sandbox flag 最小配置。
- `configs/config.yaml` 当前只配置了 `secrets.data_dir`、`secrets.master_key_env`，还没有权限策略、路径策略、网络策略和沙箱策略。

结论：

1. 应新增统一的 `policy` 配置域，表达“什么动作、什么资源、什么主体、什么环境下 allow/ask/deny”。
2. 文件系统、网络、命令、MCP、Skill、连接、Sandbox 不各自发明权限模型，而是统一转成 `approval.Request` 和 `PolicySubject/PolicyResource`。
3. CLI/Desktop/Enterprise 共享策略模型与 evaluator，差异只在配置来源、默认 posture、审批 requester、授权持久化范围、管理界面。
4. 密钥和连接只通过 `credential_ref` / `connection_ref` 引用；配置文件可以声明 provider/connection 元数据，但不应保存明文密钥。
5. `config.yaml` 应保持可提交的安全默认值；`config.local.yaml` 只适合本地开发覆盖；Enterprise 应优先使用 DB/KMS/Vault/环境注入，不依赖本地文件作为治理事实源。

## 二、配置源与优先级

推荐配置源从低到高：

| 层级 | 来源 | 适用产品 | 是否可含密钥 | 说明 |
|---|---|---|---|---|
| 1 | 内置默认值 | 全部 | 否 | 代码里的保守默认值，保证缺省安全。 |
| 2 | 产品级用户配置 `~/.genesis-agent/<product>/config.yaml` | CLI/Desktop | 否/开发期可临时含密钥 | 跨项目复用的用户偏好、常用 provider id、审批体验。 |
| 3 | 项目共享配置 `configs/config.yaml`、`llm.yaml`、`mcp.yaml` | CLI/Desktop/Enterprise dev | 否 | 可提交版本库，按职责拆分；项目约束覆盖用户默认。 |
| 4 | 项目配置 `.genesis/config.yaml` | CLI/Desktop | 否 | 规划项：工作区策略，例如允许读取某些项目外路径、默认 sandbox mode。 |
| 5 | 项目本地配置 `configs/config.local.yaml` | 本地开发 | 可以但不推荐 | 已 gitignore，覆盖项目共享配置，只用于当前开发机调试。 |
| 6 | `AGENT_` 环境变量 | 全部 | 可以 | 适合 CI、容器、临时强制覆盖、主密钥。 |
| 7 | Credential/Connection Store | 全部 | 是 | 密钥与连接事实源，文件加密/DB/Vault/KMS 可替换。 |
| 8 | 运行时授权 Grant Store | 全部 | 否 | 用户批准后的 once/turn/session/project 授权记录，只能在上层策略允许的范围内生效。 |
| 9 | Enterprise Admin Policy | Enterprise | 引用 | 租户/角色/项目/Agent 级策略，最终优先级最高，可撤销或压制运行时 grant。 |

合并原则：

- `deny` 优先于 `allow`，高优先级策略可以进一步收紧低优先级策略。
- 低优先级配置和运行时 grant 都不能解除 Enterprise Admin Policy 的 deny。
- 明文密钥只允许出现在 CredentialStore、环境变量或本地开发覆盖中；普通配置中只允许 `credential_ref`。
- 普通 `${ENV_NAME}` 占位符属于所在文件层，不是独立优先级：变量未定义时不覆盖低层值，变量已定义时使用其值；显式 `""` 或 `[]` 仍表示清空。
- 配置中的环境展开结果不写回文件；只有 `AGENT_` 环境变量构成独立的最高文件配置覆盖层。

## 三、统一策略模型

### 3.1 决策结果

统一决策仍沿用现有 `approval.model.PolicyType`：

| 决策 | 含义 |
|---|---|
| `allow` | 自动允许，不询问用户。 |
| `ask` | 需要产品侧 requester 询问用户或进入企业审批流。 |
| `deny` | 直接拒绝，不允许模型或用户在当前上下文绕过。 |

### 3.2 授权范围

现有 scope 基本合理，但语义需要统一：

| Scope | 适用场景 | 是否建议持久化 |
|---|---|---|
| `once` | 本次工具调用 | 否 |
| `turn` | 本轮用户交互内同类操作 | 会话内短暂缓存 |
| `session` | 当前会话 | 可持久化到会话状态 |
| `project` | 当前项目/工作区 | CLI/Desktop 可写项目 grant；Enterprise 需管理员允许 |
| `tenant` | 租户范围 | 仅 Enterprise 管理员策略，不由普通交互审批产生 |
| `global` | 全局用户/系统 | 仅系统/管理员策略，默认不在交互审批中提供 |

建议补充两个派生维度，但不新增为 `GrantScope`：

- `profile`：CLI/Desktop/Enterprise 或自定义 profile，作为 policy match 条件。
- `environment`：local/dev/test/prod/sandbox，作为 policy match 条件。

### 3.3 主体、动作、资源、环境

统一策略输入：

```go
type PolicySubject struct {
    TenantID  string
    UserID    string
    Roles     []string
    ProjectID string
    AgentID   string
    Product   string // cli / desktop / enterprise
}

type PolicyAction struct {
    Name     string // file.read / web.fetch / command.exec / credential.resolve
    ToolName string
    Risk     string
}

type PolicyResource struct {
    Type     string // file / directory / url / domain / command / connection / credential / sandbox
    URI      string
    Display  string
    Metadata map[string]string
}

type PolicyEnvironment struct {
    Profile     string
    WorkspaceID string
    WorkspaceRoot string
    SandboxMode string
    NetworkMode string
    RuntimeEnv  string // local/dev/test/prod
}
```

各能力域负责把自身上下文转成统一输入：

| 能力 | Resource URI 示例 | 关键 metadata |
|---|---|---|
| 文件系统 | `workspace://src/main.go`、`file:///D:/data/a.txt` | path_scope、backend、workspace_rel、operation、protected |
| 命令执行 | `command://go test ./...` | cwd、program、args_hash、network_intent、destructive |
| 网络搜索 | `web-search://query/<hash>` | query_preview、provider、domains、region |
| 网络获取 | `https://example.com/page` | domain、resolved_ip_class、redirect_host、content_type |
| HTTP API | `connection://crm_prod` 或 URL | connection_ref、method、host、path_template |
| MCP | `mcp://server/tool` | server、tool、connector_id |
| Skill | `skill://authority/package` | authority、trusted、external_dependencies |
| Credential | `credential://cred_id` | purpose、connection_ref、secret_type |
| Sandbox | `sandbox://profile/name` | provider、filesystem_mode、network_mode |

## 四、策略配置 Schema 建议

新增 `policy` 顶层配置域，第一版可以先只实现子集，但文档和结构应一次设计清楚。

```yaml
policy:
  defaults:
    unknown: ask
    dangerous: ask
    critical: deny
    deny_overrides_allow: true
    allowed_grant_scopes: [once, turn, session, project]

  grants:
    store: memory       # memory | file | db
    persist_scopes: [session, project]
    project_grants_file: .genesis/grants.yaml
    session_ttl: 12h

  files:
    default: ask
    workspace:
      read: allow
      list: allow
      walk: allow
      write: allow
      edit: allow
      delete: ask
    external:
      read: ask
      list: ask
      walk: ask
      write: ask
      edit: ask
      delete: deny
    protected:
      default: deny
    allow_paths:
      - path: D:\workspace\go
        operations: [read, list, walk]
        scope: project
      - path: D:\tmp
        operations: [read, write, edit, delete, list, walk]
        scope: session
    deny_paths:
      - path: C:\Windows
        operations: [read, write, edit, delete, list, walk]
      - path: ${USERPROFILE}\.ssh
        operations: [read, write, edit, delete, list, walk]
    workspace_metadata:
      write: deny
      paths: [.git, .agents, .codex]

  commands:
    default: ask
    allow:
      - program: go
        args_prefix: [test]
        cwd_scope: workspace
      - program: npm
        args_prefix: [run, build]
        cwd_scope: workspace
    deny:
      - program: rm
      - program: powershell
        args_contains: [Remove-Item, -Recurse, -Force]
    network:
      default: ask

  web:
    search:
      default: ask
      providers:
        searxng: allow
        brave: ask
        baidu_ai_search: ask
      allowed_domains: []
      blocked_domains: [localhost, 127.0.0.1]
    fetch:
      default: ask
      allowed_domains: []
      blocked_domains: [localhost, 127.0.0.1, 169.254.169.254]
      allow_private_ip: false
      cross_host_redirect: ask
      browser_render: ask

  connections:
    default: ask
    allow_refs:
      - connection_ref: github_public
        tools: [http_request, web_fetch]
    deny_refs: []

  credentials:
    resolve_default: deny
    allow_purposes: [llm_provider_call, connection_auth, web_provider_call]
    expose_plaintext_to_model: deny

  sandbox:
    default_mode: disabled
    required_for:
      - command.exec
      - web.fetch.browser
    allow_network: false
    allow_host_paths: []
```

关键规则：

- `allow_paths` 表示“不拦截/自动允许”的路径，但仍要经过路径解析、symlink 防护、工作区映射和审计。
- `deny_paths` 永远优先，不能被交互审批覆盖。
- `ask` 的 requester 可以给用户选择 `once/turn/session/project` 等 scope，但只能在 `allowed_grant_scopes` 内选择；`tenant/global` 不应由普通用户弹窗直接产生。
- 对外网络、命令执行、密钥解析、浏览器渲染默认不应 `allow`，除非项目或企业策略显式配置。

## 五、路径与资源匹配最佳实践

路径匹配是最容易出事故的地方，不应只做字符串前缀。

要求：

1. 所有路径配置先解析为绝对路径并 `Clean`。
2. Windows 下比较时统一大小写、处理盘符、UNC 路径、长路径前缀。
3. symlink/junction/reparse point 的最终目标要参与 deny/allow 判断。
4. allow 只对明确操作生效，例如允许 read 不代表允许 write/delete。
5. 工作区内 `.git`、`.agents`、`.codex` 等元数据默认禁止写入，但可允许只读。
6. 外部路径授权应优先建议 `once/session`，不默认建议 `project`。
7. `delete`、递归写、跨盘移动、覆盖大量文件应提升风险等级。

推荐资源匹配器：

```go
type ResourceMatcher interface {
    Match(ctx context.Context, subject PolicySubject, action PolicyAction, resource PolicyResource, env PolicyEnvironment) (MatchResult, error)
}
```

文件系统使用专用 matcher；URL/domain、command、connection、credential 也各自有 matcher，但输出统一 `PolicyResult`。

## 六、审批交互设计

### 6.1 用户可选项

当策略为 `ask` 时，产品侧 requester 应展示：

- 动作：例如 `file.write`、`web.fetch`、`command.exec`。
- 资源：路径、域名、命令、连接名。
- 风险：low/medium/high/critical。
- 原因：为什么要执行。
- 影响：读取/写入/删除/联网/发送凭据/执行命令。
- 可选授权范围。

推荐选项：

| 选项 | Decision | Scope |
|---|---|---|
| 允许本次 | `approved_for_scope` | `once` |
| 允许本轮 | `approved_for_scope` | `turn` |
| 允许本会话 | `approved_for_scope` | `session` |
| 允许本项目 | `approved_for_scope` | `project` |
| 拒绝本次 | `denied` | `once` |
| 中止任务 | `abort` | `once` |

可选但不建议默认展示：

- `tenant`：只给 Enterprise 管理员策略，不给普通交互审批。
- `global`：只给用户设置页或管理员后台，不在工具调用弹窗中展示。
- “记住对同类操作的拒绝”：需要单独 deny grant store，避免误伤后续正常操作。

### 6.2 产品差异

| 产品 | Requester | 默认 posture | 授权持久化 |
|---|---|---|---|
| CLI | 终端确认；quiet 模式自动 deny ask | 本地开发友好，但外部路径/网络/命令仍 ask | memory + 可选项目 `.genesis/grants.yaml` |
| Desktop | 弹窗/权限中心 | 更强调可视化和撤销 | 用户配置目录 + 项目 grant |
| Enterprise | 审批流/RBAC/策略引擎；一般不现场问模型操作者 | 默认最小权限，预配置 allowlist | DB，按 tenant/project/user/role 管理 |

## 七、各能力域配置边界

### 7.1 文件系统

统一放在 `policy.files`，不要在每个文件工具里写自己的 allowlist。

工具只负责：

- 解析路径。
- 识别操作类型。
- 构造 approval request。
- 执行前二次确认 freshness/lock。

策略负责：

- workspace/external/protected 分类。
- allow_paths/deny_paths。
- operation 级控制。
- 授权记忆范围。

### 7.2 命令执行与沙箱

命令执行策略应和 sandbox 配置联动：

- `command.exec` 默认 ask。
- 危险命令 deny 或 ask once，不允许 project scope。
- 需要网络的命令必须额外触发 network policy。
- `SandboxAuto` 不能静默降级为无沙箱；降级必须有 warning/trace/audit。
- `SandboxRequire` 不满足时必须失败。

### 7.3 网络搜索与网络获取

`web_search` / `web_fetch` 的 provider key 走 CredentialStore，provider 选择走配置，URL/domain 安全走 WebPolicy。

- Search query 可以 ask/allow；provider 凭据不暴露给模型。
- Fetch URL 必须做 SSRF、DNS、redirect、private IP 校验。
- Browser render、Crawl4AI、Firecrawl、Playwright 都是 backend adapter，不改变统一 policy。

### 7.4 连接与 HTTP API

`http_request` 是高风险工具，推荐只允许通过 `connection_ref` 调用业务系统。

- 直接 URL 默认 ask。
- 带 `connection_ref` 时先校验调用主体是否能使用该 connection。
- `connection.allowed_tools`、`allowed_agents` 只是连接元数据；最终授权由 policy evaluator 结合 subject 决定。

### 7.5 MCP 与 Skill

- MCP server/tool 应作为 `mcp://server/tool` 资源参与策略。
- Skill 加载本身可低风险 allow，但声明外部依赖、MCP、脚本、网络能力时必须 ask 或由管理员预授权。
- Skill 不应携带项目固定路径、Genesis 专属分层边界或真实密钥；这些应放配置和 AGENTS.md。

## 八、配置与代码目录建议

新增能力域：

```text
internal/capabilities/policy/
  contract/
    evaluator.go        # PolicyEvaluator
    model.go            # Subject/Action/Resource/Environment/Decision
    matcher.go          # ResourceMatcher
  service/
    evaluator.go        # 合并 rules、grants、product defaults
    grants.go           # GrantStore 编排
  adapter/
    config/             # 从 platform/config 映射 policy 配置
    memory/             # session grant store
    file/               # CLI/Desktop project grant store
    db/                 # Enterprise grant/rule store
  matcher/
    filesystem/
    command/
    web/
    connection/
    credential/
    mcp/
```

`approval` 与 `policy` 的关系：

```text
Tool -> BuildApprovalRequest -> ApprovalService
ApprovalService -> PolicyEvaluator -> allow/ask/deny
ask -> Product Requester -> Decision
Decision -> GrantStore -> Audit/Trace
```

`approval` 负责人机交互编排；`policy` 负责规则判断。现有 `approval/adapter/static` 可以逐步迁移为 `policy/adapter/config + matcher`。

## 九、配置文件演进计划

### Phase 1：文档与最小配置结构

- 新增 `policy` 顶层配置结构体，但先不要求完整实现全部 matcher。
- 将现有 static approval 策略映射为默认 policy。
- `config.yaml` 增加安全默认样例，保持不含密钥。
- CLI 继续支持 `--sandbox`、`--quiet`，但内部映射到 policy/default posture。

### Phase 2：文件系统、命令、网络策略落地

- 实现 filesystem matcher 和 allow_paths/deny_paths。
- 实现 command matcher。
- 实现 web domain/url matcher。
- GrantStore 支持 session/project。
- Desktop 加权限中心，CLI 加配置命令。

### Phase 3：Enterprise 治理

- DB PolicyStore / GrantStore。
- RBAC、租户、角色、项目策略。
- 审批流和审计查询。
- Connection/Credential 使用策略统一接入。
- 策略版本、变更审计、回滚。

## 十、推荐安全默认值

- 未分类动作：`ask`。
- critical/protected：`deny`。
- workspace read/list/walk：`allow`。
- workspace write/edit：开发期可 `allow`，Enterprise 默认可按项目策略 `ask`。
- workspace metadata write：`deny`。
- external path：默认 `ask`，delete 默认 `deny`。
- command.exec：默认 `ask`。
- web.fetch/web.search：CLI 可 `ask`，Enterprise 默认需 allowlist。
- credential.resolve：默认 `deny`，只允许 resolver 内部按 purpose 调用。
- browser render / Crawl4AI / Firecrawl / Playwright：默认 `ask` 或禁用。
- sandbox required 时不可静默降级。

## 十一、验证要点

后续实现配置能力时，至少测试：

- 配置优先级：default < 产品级用户配置 < config.yaml/llm.yaml/mcp.yaml < .genesis/config.yaml（规划）< config.local.yaml < AGENT_ 环境变量 < credential/connection store < runtime grant < admin policy。
- deny 覆盖 allow。
- Windows 路径大小写、盘符、symlink/junction。
- once/turn/session/project grant 命中与过期。
- quiet 模式下 ask 自动 deny。
- Enterprise 下普通用户不能提升到 tenant/global。
- Credential 明文不进入日志、Trace、工具输出。
- SandboxRequire 降级失败。


## 十二、代码与文档对齐审计结论 (Code-Doc Alignment)

本章节记录当前代码实现与本文档设计目标的对齐情况。审计范围以当前仓库代码为事实，不把规划、结构占位或测试桩视为完整实现。

### 1. 审计元信息 (Audit Meta)

- **审计参考源**：本文档一至十一章，以及 `docs/统一配置权限与审批治理实现计划.md`。
- **代码审查范围**：
  - `internal/platform/config/config.go`、`configs/config.yaml`
  - `internal/capabilities/policy/`
  - `internal/capabilities/approval/`
  - `internal/capabilities/filesystem/permission/`
  - `internal/capabilities/execution/policy/`、`internal/capabilities/execution/tool/run_command/`
  - `internal/capabilities/web/`
  - `products/cli/bootstrap/container.go`、`products/cli/internal/approval/requester.go`
- **主要假设**：当前审计关注 CLI 与通用能力域的 Phase 1 / Phase 2 初始落地；Desktop、Enterprise、DB/RBAC/Admin Policy、Connection/Credential/MCP 统一治理仍按后续阶段处理。

### 2. 进度对照表 (Progress Comparison Table)

| 需求 / 设计要点 | 对应文档章节 | 实现证据与文件路径 | 状态 | 差距与备注 |
| :--- | :--- | :--- | :---: | :--- |
| 顶层 `policy` 配置域 | 一、问题与结论；四、策略配置 Schema 建议 | `internal/platform/config/config.go` 中 `PolicyConfig`；`configs/config.yaml` 中 `policy:` | `[x] Implemented` | 已有顶层配置、默认配置、基础校验。 |
| 配置源与优先级 | 二、配置源与优先级 | `config.LoadForProduct` / `LoadWithOptions` 支持用户级、`config.yaml` / `llm.yaml` / `mcp.yaml`、`config.local.yaml`、普通环境变量占位回退与 `AGENT_` 显式覆盖；`LoadHookConfig` 使用同一用户 < 项目 < local 语义 | `[/] Partially implemented` | 当前文件层级已实现；尚未实现通用项目 `.genesis/config.yaml`、Credential/Connection Store 合并、Runtime Grant Store、Enterprise Admin Policy。 |
| 决策结果与授权范围 | 三、统一策略模型 | `approval/model/model.go` 定义 `allow/ask/deny` 与 `once/turn/session/project/tenant/global`；`policy/service/evaluator.go` 过滤普通 suggested scopes | `[/] Partially implemented` | 类型已完整定义；但通用 approval store 仍只缓存 session，turn/project/tenant/global 的通用 GrantStore 未实现。 |
| 策略输入模型 | 三、统一策略模型 | `policy/model/model.go` 有 `Subject`、`Environment`；实际 evaluator 接收 `approvalmodel.Request` | `[/] Partially implemented` | Subject/Environment 只是占位，运行时判断仍主要依赖 request metadata；Enterprise 多租户/角色/Agent 维度未接入。 |
| policy 能力域目录 | 八、配置与代码目录建议 | `internal/capabilities/policy/{contract,service,adapter,matcher,model}` | `[/] Partially implemented` | 核心骨架已建；尚无 `grants.go`、file/db GrantStore、command/web/connection/credential/mcp matcher。 |
| 默认 policy 替代 static policy | 九、Phase 1 | `policy/service/evaluator.go` 中 matcher + `metadataFallback`；`policy/adapter/approval/engine.go` | `[x] Implemented` | 已能适配现有 `approvalcontract.PolicyEngine`，并保留 trusted/workspace/dangerous/external/critical 的旧 metadata 行为。 |
| CLI 接入统一 policy | 六、审批交互设计；九、Phase 1 | `products/cli/bootstrap/container.go` 加载 `cfg.Policy`，通过 `policyconfig.BuildEvaluator` 和 `policyapproval.NewEngine` 构建 approval service | `[x] Implemented` | CLI 已接入；但当前 CLI bootstrap 会与 shared builder 各加载一次 config，后续应收敛为产品级配置上下文。 |
| 文件系统审批请求构造 | 七、文件系统 | `filesystem/permission/permission.go` 构造 `approvalmodel.Request`；文件工具经 `RuntimeFilePermissions` 包装 approval service | `[x] Implemented` | 文件工具已走统一审批请求和运行时文件授权缓存。 |
| 文件系统 matcher 与路径策略 | 五、路径与资源匹配最佳实践 | `policy/matcher/filesystem/matcher.go` 支持 workspace/external/protected、allow_paths、deny_paths、operation 匹配、Windows 小写、路径边界 | `[/] Partially implemented` | 已实现核心匹配；`workspace_metadata.paths` 配置已参与写/改/删拦截，并保留 `permission.go` 对 `.git/.agents/.codex` 的默认硬保护。仍缺 symlink/junction/reparse point 最终目标校验。 |
| 命令执行 policy 接入 | 七、命令执行与沙箱 | `execution/policy/policy.go` 构造命令审批 metadata；policy evaluator 通过 `metadataFallback` 兼容危险/外部/critical 判断 | `[/] Partially implemented` | 命令会进入 approval；但 `policy.commands.default/allow/deny` 配置未被命令 matcher 消费，网络意图与 command allow/deny 细则未实现。 |
| 沙箱策略接入 | 七、命令执行与沙箱；十、推荐安全默认值 | `PolicySandboxConfig` 仅解析 `default_mode`；CLI `--sandbox` 仍走 `products/cli/internal/sandbox` | `[ ] Unimplemented` | `policy.sandbox` 未接入 `shared/local/sandbox.Manager`，也未覆盖 CLI sandbox flag；SandboxRequire 失败闭环由 execution/sandbox 层处理，不属于统一 policy 配置结果。 |
| 网络搜索 / 网络获取 policy 接入 | 七、网络搜索与网络获取 | `internal/capabilities/web` 已有 `web_search` / `web_fetch` 工具与 provider；未发现 approval 注入 | `[ ] Unimplemented` | 工具存在，但 `policy.web.search/fetch` 未参与运行时审批，SSRF/DNS/redirect/private IP policy 未在统一 matcher 中实现。 |
| 连接 / 凭据 / MCP policy 接入 | 七、连接与 HTTP API；七、MCP 与 Skill | 未发现 `connection.use`、`credential.resolve`、`mcp://server/tool` 统一 matcher 或 resolver policy 调用 | `[ ] Unimplemented` | 仍需在 Connection/Credential resolver、MCP 调用入口接入 policy evaluator。 |
| Skill policy 接入 | 七、MCP 与 Skill | `Skill`、`read_skill_resource` 通过 approval service；catalog/request metadata 可进入统一审批 | `[/] Partially implemented` | Skill 工具已接 approval，但外部依赖、MCP、脚本、网络能力等细粒度 policy matcher 尚未实现。 |
| CLI requester 交互 | 六、审批交互设计 | `products/cli/internal/approval/requester.go`；quiet 模式使用 deny requester | `[/] Partially implemented` | CLI 终端交互已有；Desktop 权限中心、Enterprise 审批流/RBAC 未实现。 |
| GrantStore 与授权持久化 | 二、配置源与优先级；三、授权范围；六、审批交互设计 | `approval/adapter/memory` 只提供通用 session cache；`filesystem/permission/runtime.go` 支持文件 session/project grant | `[/] Partially implemented` | 文件域有 runtime grant；统一 GrantStore、project grant 文件、tenant/global 管理策略未实现。 |
| Enterprise Admin Policy | 二、配置源与优先级；Phase 3 | 无 DB PolicyStore / RBAC / admin policy 证据 | `[ ] Unimplemented` | 仍是 Phase 3 规划。 |
| 测试覆盖 | 十一、验证要点 | `config_test.go`、`hook_config_test.go`、`policy/service/evaluator_test.go`、`policy/matcher/filesystem/matcher_test.go`、`products/cli/bootstrap/sandbox_behavior_test.go` | `[/] Partially implemented` | 已覆盖用户 < 项目共享 < 项目 local < `AGENT_`、未定义占位符回退、Hook 来源顺序、非法配置、deny 优先、scope 过滤、文件 matcher、workspace metadata 配置路径、CLI sandbox 行为；缺少 symlink/junction、web policy、command matcher、GrantStore、Enterprise 场景测试。 |

### 3. 差距反思 (Gap Reflections)

- **实现差距**：当前已形成 CLI + config + policy evaluator + filesystem matcher 的第一批闭环，但命令、网络、连接、凭据、MCP、Enterprise 治理仍主要依赖旧 metadata 或尚未接入统一 policy。
- **文档修正**：旧审计段中“路径匹配与文件系统 matcher 完美对齐”“命令执行能力域接入已实现”的判断偏高。按当前代码证据，应分别降级为 `Partially implemented`。
- **架构取舍**：当前 `Matcher` 直接基于 `approvalmodel.Request`，比原设计中的 `Match(ctx, subject, action, resource, env)` 更轻量，适合 Phase 1；但这会推迟 Subject/Environment 的治理能力，后续进入 Enterprise 时必须补齐。
- **安全风险**：`policy.web.*` 已出现在配置里，但 web 工具尚未调用 approval，这容易让读者误以为网络已被统一拦截。当前文档和配置注释必须持续强调它只是预留字段。

### 4. 后续演进建议与行动项 (Actionable Next Steps)

1. **P0：接入 WebPolicy**
   - 文件：`internal/capabilities/web/tool/web_search`、`internal/capabilities/web/tool/web_fetch`、`internal/capabilities/policy/matcher/web`。
   - 验收：`policy.web.fetch.default=ask` 时，CLI 调用 fetch 进入 approval；localhost/private IP/redirect 有测试。

2. **P0：补齐 command matcher**
   - 文件：`internal/capabilities/policy/matcher/command`、`internal/capabilities/execution/policy`、`run_command` 测试。
   - 验收：`policy.commands.allow/deny` 能控制 program、args_prefix、cwd_scope；危险命令不能被 project scope 放宽。

3. **P1：补齐 symlink/junction/reparse point 最终目标校验**
   - 文件：`shared/local/pathresolver`、`internal/capabilities/filesystem/permission/permission.go`、`internal/capabilities/policy/matcher/filesystem/matcher.go`。
   - 验收：allow/deny_paths 与 workspace metadata 规则同时检查用户输入路径和最终真实目标；Windows junction/reparse point 有测试覆盖。

4. **P1：实现通用 GrantStore**
   - 文件：`internal/capabilities/policy/service/grants.go`、`adapter/memory`、`adapter/file`。
   - 验收：turn/session/project grant 命中与过期可测；deny 仍覆盖已有 grant。

5. **P2：Enterprise Admin Policy 骨架**
   - 文件：`products/enterprise/bootstrap`、`internal/capabilities/policy/adapter/db`。
   - 验收：tenant/project/role policy 能压制 runtime grant，审计记录策略来源。

